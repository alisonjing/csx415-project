
#' This is load packages function
#'
#' This function allows you to load all the required packages
#' required for forest fire burned area relative to factor variables of prediction model.
#' @param none
#' @keywords
#' @export
#' @examples
load_packages <- function() {
    library(ggplot2)
    library(magrittr)
    library(tidyverse)
    library(knitr)
    library(caret)
    library(rpart)
    library(rpart.plot)
    library(devtools)
    library(devtools)
    library(randomForest)
    library(tree)
    library(RColorBrewer)
    library(ROCR)
    library(e1071)
   }
